# Cloud Functions for Aesthetics of Exclusion

First, install the [Firebase CLI](https://firebase.google.com/docs/cli#mac-linux-npm):

    npm install -g firebase-tools

To deploy, run:

    firebase deploy
